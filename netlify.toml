[build]
  base    = "frontend"
  command = "npm run build"
  publish = "dist"

# SPA fallback — all routes serve index.html (React Router handles the rest)
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
