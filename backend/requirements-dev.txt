# Development-only dependencies (backend test suite).
#
# Install:
#   py -3 -m pip install -r backend/requirements.txt -r backend/requirements-dev.txt
# Run:
#   py -3 -m pytest backend/tests -q

# Test runner for the P2/P5 backend test suite.
pytest==8.3.3

# Required by fastapi.testclient.TestClient (API smoke tests).
# Pinned to 0.27.x deliberately: starlette 0.38.x (fastapi 0.115) breaks with
# httpx >= 0.28, which removed the `app=` shortcut TestClient relies on.
httpx==0.27.0
